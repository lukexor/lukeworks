//! Contact page

use leptos::{component, view, IntoView, Scope};

/// Contact page of `lukeworks.tech`.
#[component]
pub fn Contact(cx: Scope) -> impl IntoView {
    view! { cx,
        "Contact"
    }
}
