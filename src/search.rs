//! Search page

use leptos::{component, view, IntoView, Scope};

/// Search page of `lukeworks.tech`.
#[component]
pub fn Search(cx: Scope) -> impl IntoView {
    view! { cx,
        "Search"
    }
}
